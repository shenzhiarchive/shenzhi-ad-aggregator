---
description: "广告实例管理模块开发规范"
alwaysApply: true
---

# 广告 SDK 初始化管理模块开发规范

## 架构要求

1. **必须使用单例模式**
   - SDK 初始化管理器必须采用单例模式实现
   - 确保全局只有一个初始化管理器
   - 管理器生命周期与应用程序生命周期绑定

2. **原生层实现**
   - 所有 SDK 初始化逻辑必须在原生层（Android/iOS）实现
   - JS 层仅通过 Native Module 接口调用初始化方法
   - 必须兼容 React Native 新架构（TurboModules）

3. **类型安全**
   - 所有接口必须使用 TypeScript 定义
   - 通过 `@react-native/codegen` 生成类型声明
   - 提供完整的类型检查和自动补全支持

## 模块职责

广告 SDK 初始化管理模块应负责以下功能：

1. **SDK 初始化**
   - 初始化广告 SDK（如穿山甲 SDK）
   - 接收应用 ID 和配置参数
   - 确保 SDK 只初始化一次
   - 管理初始化状态

2. **初始化状态管理**
   - 跟踪 SDK 初始化状态（未初始化、初始化中、已初始化）
   - 提供初始化状态查询接口
   - 防止重复初始化

3. **错误处理与日志**
   - 记录初始化过程和结果
   - 处理初始化失败的情况
   - 提供错误回调机制

## 必须遵循

1. **单次初始化原则**
   - SDK 初始化只能执行一次
   - 重复调用初始化方法应返回已初始化状态或错误提示
   - 初始化状态必须在原生层持久化

2. **线程安全**
   - 初始化操作必须在主线程执行
   - 使用适当的同步机制保护初始化状态
   - 避免并发初始化导致的问题

3. **资源管理**
   - 初始化时正确管理 Context/Application 引用
   - 避免持有不必要的强引用
   - 确保初始化资源正确释放

4. **接口设计**
   - JS 层仅暴露一个初始化方法
   - 提供必要的错误处理和状态反馈
   - 支持 Promise/回调两种异步模式

## 严格禁止

1. **禁止在 JS 层管理初始化状态**
   - JS 层不允许维护初始化状态
   - 不允许在 JS 层缓存初始化结果
   - 所有初始化状态管理必须在原生层完成

2. **禁止重复初始化**
   - 不允许多次调用初始化方法
   - 初始化前必须检查是否已初始化
   - 提供明确的错误提示

3. **禁止在 JS 层缓存 Android Context**
   - Context 管理必须在原生层完成
   - JS 层不允许持有任何 Context 引用
   - 初始化时从原生层获取 Context

4. **禁止暴露其他接口**
   - 不允许暴露实例管理相关接口
   - 不允许暴露广告创建相关接口
   - 只允许暴露初始化相关接口

5. **禁止跨线程初始化**
   - 不允许在非主线程执行初始化
   - 不允许在后台线程访问初始化状态
   - 所有初始化操作必须确保线程安全

## 接口规范

### JS 层接口示例

```typescript
interface AdInstanceManager {
  // 初始化广告 SDK
  initialize(
    appId: string,
    config?: SDKConfig
  ): Promise<void>;
}
```

### 初始化方法规范

- **方法名**：`initialize`
- **参数**：
  - `appId`：广告平台应用 ID（必填）
  - `config`：SDK 配置参数（可选）
- **返回值**：`Promise<void>`
- **行为**：
  - 如果 SDK 已初始化，应返回成功或抛出明确的错误
  - 如果初始化失败，应通过 Promise reject 或回调返回错误信息
  - 初始化过程应异步执行，不阻塞 JS 线程

